# Quick run

1. Install dependencies:
   pip install -r requirements.txt

2. Start the backend:
   python serve_frontend.py

3. Open the frontend page:
   index.html

4. Upload any supported file and ask a question.
